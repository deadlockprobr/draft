# Server
PORT=3000
NODE_ENV=development

# Public base URL (used for cover URLs, webhooks, OG images)
BASE_URL=http://localhost:3000

# Deadlock Heroes API (NEXT_PUBLIC_ prefix makes it available on the client)
NEXT_PUBLIC_HEROES_API_URL=https://assets.deadlock-api.com/v2/heroes
